DATABASE_URL="postgresql://postgres:postgres@localhost:5432/das_nepal?schema=public"

# Bearer token required to call GET /api/inquiries (admin/internal-only).
# If unset, the GET endpoint returns 401 unconditionally.
INQUIRIES_ADMIN_TOKEN="change-me-to-a-long-random-string"
